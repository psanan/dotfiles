# Escape: Use control-^ (control-shift-6) instead of control-a
escape ^^^

# vertically split, change focus, and start a new session with escape-s
bind s eval "split -v" "focus" "screen"

# use 'x' to kill the current region instead of locking the screen
bind x remove

# I don't know why, but often I have to manually turn mousetrack back on (escape : mousetrack on)
mousetrack on
defmousetrack on

startup_message off
altscreen on

hardstatus alwaysfirstline
hardstatus string '%{bk} GNU screen: %S %{dk} %{w}%?%-Lw%?%{=b kR}(%{W}%n*%f %t%?(%u)%?%{=b kR})%{= w}%?%+Lw%?%? %{g}]'
